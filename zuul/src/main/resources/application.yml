server:
  port: 8888

spring:
  application:
    name: zuul

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

zuul:
  routes:
    web:
      path: /web/**
      serviceId: web
    feigin:
      path: /feigin/**
      serviceId: feigin
